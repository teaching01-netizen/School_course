# Diagnostics

Place small, runnable debugging/support scripts here.

Rules:

- Prefer a script over a long “how to debug” paragraph.
- Scripts should print actionable output and exit non-zero on failure.
- When a script is added to support a real failure, record it in `docs/failure-cases.md`.

