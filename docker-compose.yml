services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: warwick_local_fresh
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d warwick_local_fresh"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - warwick_pgdata:/var/lib/postgresql/data

volumes:
  warwick_pgdata:

