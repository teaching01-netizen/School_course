# Contributing — see `../CONTRIBUTING.md`

This file exists so `backend/` contributors find the policy without hunting.
All migration and backfill rules live at the repo root:

- **Policy:** [`../CONTRIBUTING.md`](../CONTRIBUTING.md) (migrations = DDL + seed-config only; backfills in `internal/backfill/`)
- **Governance:** [`../GOVERNANCE.md`](../GOVERNANCE.md)
- **Lint:** `bash ../scripts/validate-migrations.sh [--strict]`
